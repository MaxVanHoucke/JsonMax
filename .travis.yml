language: cpp
compiler: gcc

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++

script:
  - mkdir build
  - cd build
  - cmake ..
  - make all
  - cd test
  - ./JsonMaxTests
